import { FC } from "react";

const Projects: FC = () => {
  return (
    <section className="container">
      <div>
        <h2>Ready for every project</h2>
      </div>
    </section>
  );
};

export default Projects;
